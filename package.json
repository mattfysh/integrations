{
  "name": "integrations",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && echo 'console.log(window)' > dist/main.js && mv district.json dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "github": "0.1.13",
    "browserify": "^12.0.0"
  }
}
